export const codeSnippets = [
  'const score = 100;',
  'let total = price * count;',
  'const double = (n) => n * 2;',
  'console.log("Hello, world!");',
  'if (ready) { start(); }',
  'const names = ["Ada", "Sam"];',
  'items.map((item) => item.name);',
  'const active = users.filter((user) => user.active);',
  'for (let i = 0; i < 5; i++) { run(i); }',
  'function add(a, b) { return a + b; }',
  'const { name, age } = profile;',
  'const copy = [...items];',
  'const found = values.includes(42);',
  'while (count > 0) { count--; }',
  'const message = `Hi, ${name}!`;',
  'const settings = { sound: false, time: 30 };',
  'const result = value ?? "default";',
  'const length = text.trim().length;',
  'if (a !== b) { return false; }',
  'const sum = numbers.reduce((a, b) => a + b, 0);',
] as const

// Original sentences for practice, not quotations attributed to real people.
export const practiceQuotes = [
  'Small steps can carry you farther than a perfect plan.',
  'Find a steady rhythm, and let the next word follow.',
  'A quiet morning is a good place to begin again.',
  'Every mistake is a chance to slow down and learn.',
  'The forest grows one leaf at a time, just like progress.',
  'Keep your eyes ahead and your hands relaxed.',
  'Some days are for speed; others are for patience.',
  'You do not have to hurry to move forward.',
  'Make room for curiosity, even in familiar places.',
  'A little practice today makes tomorrow feel easier.',
  'When the path bends, take a breath and keep going.',
  'Good habits begin with something small enough to repeat.',
  'Let your attention return to the word in front of you.',
  'There is no shortcut to a skill you enjoy building.',
  'The best pace is one you can return to tomorrow.',
  'Try again, and bring what you learned along with you.',
  'A clear thought can begin with a single simple sentence.',
  'Leave a little space between effort and expectation.',
  'Your next attempt does not need to look like your last.',
  'Stay curious; the ordinary world still has surprises.',
] as const
